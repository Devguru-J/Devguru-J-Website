---
import { content, type Lang } from '../i18n';
const { lang = 'en' } = Astro.props as { lang?: Lang };
const t = content[lang];
---

<aside class="ocm-panel" aria-label="Navigation Menu">
  <nav>
    {t.menu.map((l) => <a href={l.href}>{l.label}</a>)}
  </nav>
</aside>
